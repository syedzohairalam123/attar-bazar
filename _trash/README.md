# Trash Folder

This folder holds any file that gets superseded or removed during
development. Nothing is ever permanently deleted from the project —
if a page or component is replaced by a newer version, the old one
is moved here (with a note) instead of being erased, so you can always
recover it if needed.

Currently empty — this is a fresh, clean build with no superseded files.
